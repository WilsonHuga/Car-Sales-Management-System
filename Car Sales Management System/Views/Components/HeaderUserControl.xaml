<UserControl x:Class="Car_Sales_Management_System.Views.Components.HeaderUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="1200">
    <UserControl.Resources>
        <!-- Menu Item Style -->
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter"
                                                 Content="{TemplateBinding Header}"
                                                 TextBlock.Foreground="{TemplateBinding Foreground}"
                                                 VerticalAlignment="Center"
                                                 HorizontalAlignment="Left"/>
                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Placement="Bottom"
                                       VerticalOffset="2"
                                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                                       Focusable="False"
                                       PopupAnimation="Fade">
                                    <Border Background="White"
                                            BorderBrush="#CCCCCC"
                                            BorderThickness="1"
                                            Padding="2">
                                        <StackPanel IsItemsHost="True"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Box Style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                             Margin="{TemplateBinding Padding}"
                                             VerticalAlignment="Center"/>
                                <TextBlock x:Name="PlaceholderText"
                                          Text="Search cars..."
                                          Foreground="#808080"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False"
                                          Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Application Title Style -->
        <Style x:Key="AppTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Icon -->
            <ColumnDefinition Width="*"/>
            <!-- Menu -->
            <ColumnDefinition Width="250"/>
            <!-- Search -->
            <ColumnDefinition Width="Auto"/>
            <!-- Title -->
        </Grid.ColumnDefinitions>

        <!-- Application Icon -->
        <Border Grid.Column="0" 
                Width="24" 
                Height="24" 
                Margin="8,3,4,3"
                Background="#000046"
                CornerRadius="2">
            <Path Fill="White" 
                  Data="M12 2L3 7v10c0 5.55 3.84 9.739 9 10.93 5.16-1.191 9-5.38 9-10.93V7l-9-5zm0 11.5l-5-5 1.41-1.41L12 10.67l8.59-8.58L22 3.5l-10 10z"
                  Width="12" 
                  Height="12"
                  HorizontalAlignment="Center" 
                  VerticalAlignment="Center"/>
        </Border>

        <!-- Menu Items -->
        <Menu Grid.Column="1" Background="Transparent" VerticalAlignment="Center" Height="30">
            <MenuItem Header="File" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="New Inventory" Command="{Binding NewInventoryCommand}"/>
                <MenuItem Header="Open Database..." Command="{Binding OpenDatabaseCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="Inventory" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Add New Car" Command="{Binding AddNewCarCommand}"/>
                <MenuItem Header="Update Pricing" Command="{Binding UpdatePricingCommand}"/>
            </MenuItem>
            <MenuItem Header="Help" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="User Manual" Command="{Binding UserManualCommand}"/>
                <MenuItem Header="Contact Support" Command="{Binding ContactSupportCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Search Box -->
        <Grid Grid.Column="2" VerticalAlignment="Center" Margin="10,0" Height="28">
            <TextBox x:Name="SearchTextBox"
                     Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8,4,24,4"/>
            <Button Width="20" 
                    Height="20" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="{x:Null}"
                    Margin="0,0,4,0">
                <Path Fill="#666666"
                      Data="M15.5 14h-.79l-.28-.27A6.5 6.5 0 1 0 13 13.77l.27.28v.79l5 4.99L17.49 20l-4.99-5zm-6 0A4.5 4.5 0 1 1 14 9.5 4.5 4.5 0 0 1 9.5 14z"
                      Stretch="Uniform"/>
            </Button>
        </Grid>

        <!-- Application Title -->
        <TextBlock Grid.Column="3" 
                   Text="{Binding ApplicationTitle, FallbackValue='AutoMart'}"
                   Style="{StaticResource AppTitleStyle}"/>
    </Grid>
</UserControl>